#include<stdio.h>
main()
{
	//char str[]= "ANMOL Diwedi";                                                   //-1 
	//char *ptr=str;              // Here we can only use *ptr="Anmol Diwedi" too!!!//-2
	
	char str[]="ANMOL Diwedi";    // we can use this also and 1 or 2 too...!!!
	printf("%s", str);           // %s is use for strings!!!
}
